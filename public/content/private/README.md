# Private 内容（本地真名版）

此目录用于放置本地私有内容文件（例如包含真实公司名的文案）。默认情况下，这些文件会被 `.gitignore` 忽略，避免误提交。

要启用 Private 版本，请创建以下两个文件：

- `public/content/private/scenes.zh-CN.json`
- `public/content/private/cards.zh-CN.json`

你可以从 Public 版本复制一份作为起点，再替换名词，但请不要写入真实内部指标、内部系统名、团队/个人信息等敏感内容。

